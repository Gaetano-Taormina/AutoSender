[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src/backend"]
addopts = "-v --cov=src/backend --cov-report=html:tests/coverage --cov-report=term-missing"
